import { NgModule } from '@angular/core';

import { Contador } from './components/contador.components';

@NgModule({
  imports: [],
  exports: [Contador],
  providers: [],
  declarations: [Contador],
})
export class ContadorModule { }
